{
  "name": "maxxed-engineering-control-plane",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/service/main.js",
    "test": "node --test",
    "validate:completion": "node scripts/validate-completion.mjs",
    "validate:training": "node scripts/validate-training-data.mjs",
    "validate:resources": "node scripts/validate-github-resource-catalog.mjs",
    "promote:model": "node scripts/promote-model.mjs",
    "validate:reuse-gate": "node scripts/check-reuse-gate.mjs",
    "reuse:metrics": "node scripts/reuse-metrics.mjs",
    "validate": "npm test && npm run validate:completion && npm run validate:training && npm run validate:resources"
  },
  "dependencies": {
    "pg": "^8.23.0"
  }
}
